apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-exam.fullname" . }}-nginx
spec:
  type: NodePort
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: {{ include "helm-exam.name" . }}
    component: nginx