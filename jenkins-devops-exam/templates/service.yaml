apiVersion: v1
kind: Service
metadata:
  name: {{ include "jenkins-devops-exam.fullname" . }}
  labels:
    {{- include "jenkins-devops-exam.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jenkins-devops-exam.selectorLabels" . | nindent 4 }}
